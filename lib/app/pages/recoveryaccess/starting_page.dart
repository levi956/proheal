import 'package:flutter/material.dart';

// here is probab

class AccessRecoveryStart extends StatelessWidget {
  const AccessRecoveryStart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
